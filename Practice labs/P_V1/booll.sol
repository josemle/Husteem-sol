// SPDX-License-Identifier: MIT
pragma solidity 0.8.0;

contract booll {
    function apple(int figure) pure public returns (bool ) {
    bool units = true;
    if (figure > 1) {
        return units;
    }
    units = false;
    return units;
    }
}